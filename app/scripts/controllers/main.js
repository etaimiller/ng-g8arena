'use strict';

/**
 * @ngdoc function
 * @name ngG8arenaApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the ngG8arenaApp
 */
angular.module('ngG8arenaApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
