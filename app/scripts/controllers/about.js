'use strict';

/**
 * @ngdoc function
 * @name ngG8arenaApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the ngG8arenaApp
 */
angular.module('ngG8arenaApp')
  .controller('AboutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
